package test.intetics;

import org.junit.Test;

import java.util.ArrayList;

import static org.junit.Assert.assertEquals;

public class SimpleTest {
/*
    @Test
    public void isSimple1() throws Exception {
        assertEquals(new Simple(1).is(), true);
    }
    @Test
    public void isSimple2() throws Exception {
        assertEquals(new Simple(2).is(), true);
    }
    @Test
    public void isSimple3() throws Exception {
        assertEquals(new Simple(3).is(), true);
    }
    @Test
    public void isSimple4() throws Exception {
        assertEquals(new Simple(4).is(), false);
    }
    @Test
    public void isSimple5() throws Exception {
        assertEquals(new Simple(5).is(), true);
    }
    @Test
    public void isSimple999() throws Exception {
        ArrayList<Integer> simpleList = new ArrayList<>();
        long start = System.nanoTime();
        for (int i=10000; i<=99999; i++) {
            if (new Simple(i).is()) {
                simpleList.add(i);
            }
        }
        long finish = System.nanoTime();
        System.out.printf("Time to find simple list: %dms, Simple list size:%d\n", (finish-start)/1_000_000,simpleList.size());
    }

*/
}